// Start coding here

let findMinNumber = function (a, b) {
  const resultMin = Math.min(a, b);
  return resultMin;
};

const minNumber = findMinNumber(10, 20);
console.log(minNumber); // 10

console.log(Math.min(10, 20));
